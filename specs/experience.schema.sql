CREATE TABLE "Experience" (
    "id" VARCHAR,
    "title" VARCHAR,
    "timestamp" TIMESTAMPTZ,
    "status" VARCHAR,
    "skill_used" VARCHAR,
    "skill_version" VARCHAR,
    "task" VARCHAR,
    "error_code" VARCHAR,
    "context" VARCHAR,
    "run" VARCHAR
);
